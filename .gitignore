build/
.vs/
.vscode/
*.exe
*.pdb
data/
chaos_run/
scratch_cmake.txt
*.bin
*.ndb
__pycache__/
*.pyc
.venv/
venv/

# Experiment output: one directory per run, CSV samples plus metadata.
# Regenerated by sweep.py; the aggregate is what gets written up.
research/replica_recall/results*/
research/replica_recall/forensics_events/
research/replica_recall/forensics_results.json

# qdrant_run_experiment.py's default scratch output. Only these four files --
# the named *_pilot* files beside them are deliberate evidence and stay
# tracked. Untracked because PR #18 and PR #24 each committed the last run's
# scratch output by accident, so main carried an unlabeled stale run for two
# merges (found in PR #27 review). Evidence is MOVED to a named directory, which
# is what qdrant_sweep.py does.
research/cross_system_replication/results/samples.csv
research/cross_system_replication/results/events.json
research/cross_system_replication/results/run_meta.json
research/cross_system_replication/results/telemetry.csvresearch/qdrant_gated_index_recall/*.log
research/qdrant_index_recall_healing/*.log
research/qdrant_index_recall_healing/*.err
research/qdrant_index_recall_healing/*.pid
research/qdrant_chaos_loop_timeout/*.log
research/qdrant_chaos_loop_timeout/*.err
research/weaviate_probe/weaviate_run/
research/weaviate_probe/*.log
research/weaviate_repair_window/*.log
