# tests/CMakeLists.txt
project(NanoDBTests)

# If you are using GoogleTest, you would add logic here.
# For now, we will leave it empty or just add a dummy executable
# so CMake stops complaining.

# add_executable(run_tests test_main.cpp)