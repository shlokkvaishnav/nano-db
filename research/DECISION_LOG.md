# Research decision log

Why things are the way they are, so the project doesn't depend on anyone's memory of it. Newest first. Add an entry whenever a research decision is made — a baseline chosen, a metric changed, a hypothesis rejected, a branch merged or archived — not just when something goes wrong.

---

**Scored a detector that had been recording its own answers, unexamined, through the entire Qdrant programme — and found it at chance on the axis that matters most.**
PR #57 (`research/loo_agreement_qdrant/`, issue #52). `loo_agreement` and `shard_agreement` are written into every `samples.csv` this project produces, including all the Qdrant studies: 51 runs, 3,594 populated rows, none zero. Nobody had ever scored them, because nothing in the pipeline's role selection looks at *data* — queries 0–5 all look at issues and PRs, so committed-but-unexamined evidence is invisible to the loop no matter how long it runs. That gap is now query 3b. **Zero compute** produced two results. **(1) Layer 3 replicates on a second system**, for end-to-end quality: chaos 0.908 against a 0.635 no-chaos control, p = 0.0023 by seeded permutation. **(2) On graph quality it is at chance**: 0.348 against a chance line of 0.333, versus a 0.670 baseline, separating at p = 0.0001 *in the wrong direction*. That second result is a limit on this project's own headline — the established Qdrant finding is replica-level `index_recall` divergence, and the ground-truth-free detector cannot find the replica that finding is about. Offered as hypothesis, not conclusion: `loo_agreement` compares *returned results*, so a replica missing objects is easy to flag while a replica holding all the data with a slightly worse graph returns nearly the same answers — the detector is subject to the same silence the project exists to study. Three things are recorded deliberately. **(a) The metric was not chosen after seeing the data**: `_detection_stats()` and `resolution_eps()` are imported unmodified from `replica_recall/analyze.py`, written for nano-db before any Qdrant run existed, and the SPEC was committed before any Qdrant detection number was computed. That is what makes a post-hoc analysis admissible. **(b) The no-chaos baseline is load-bearing and was pre-registered as such**: at 0.635 against a 0.333 chance line, a healthy cluster scores well above chance with nothing to detect, purely from near-ties surviving the exclusion rule — quoting the chaos figure against *chance* rather than *baseline* would have overstated the detector badly. **(c) Review caught a grouping bug that changed the conclusion**: `condition_of()` classified by substring, and `"chaos" in "nochaos"` is True, so five `..._nochaos_...` runs were scored as chaos runs. Reading the condition from each run's own `run_meta.json` instead moved the `index_recall` result from a weak null (0.469 vs 0.648, p = 0.0594) to an unambiguous collapse to chance (0.348 vs 0.670, p = 0.0001). The fix made both halves stronger. Also deviates from project practice for a stated reason: the groups are 9–20 runs, so C(25,12) = 5.2M makes exact enumeration infeasible and a seeded permutation test (200k resamples) is used, labelled per row in the output. See `research/loo_agreement_qdrant/SPEC.md`.

**Withdrew "Weaviate's repair is two discrete paths" one day after publishing it, and voided the pre-registered statistic that was meant to test it.**
PR #51 (`research/weaviate_repair_window/`, issue #48, step 2c). #49 reported repair latency as bimodal with "nothing between 0.2 s and 36 s across 18 runs" and read that as two discrete code paths. Step 2c fixed the timing offsets instead of randomizing the pre-restart delay and put **12 of 18 observations inside that interval**; the gap was an artifact of how a uniform random delay samples the latency surface, not a property of Weaviate. Two process points are recorded rather than smoothed. **(1) The pre-registration encoded the assumption it was meant to test.** The step 2c metric fixed a binary fast/slow split at 1 s *because* the gap was believed real; with the gap gone that threshold cuts condition B's tight 0.735–2.245 s cluster in half, so the "slow 4/6" and Fisher p = 0.4545 the script prints are artifacts of a dead premise. Pre-registration stopped motivated reading of the result and did not stop a bad analysis choice from being locked in — the defence is to pre-register the *estimand* and keep the dichotomization out of it. **(2) The nominated comparison was the wrong one.** A-vs-C was named discriminating; B-vs-C is the clean contrast, because it holds absence fixed and varies only divergence age. What replaces the withdrawn claim is stronger: at a fixed 40 s outage, a divergence ~38 s old reconciles in ~1 s while one ~6 s old takes ~31 s — disjoint, exact two-sided Mann-Whitney U = 0 at 6v6, p = 0.0022 — and an earlier draft called this "an older divergence repairs faster" -- **withdrawn in review round 1**, because measuring from the write rather than the restart puts B and C at 38.9 s and 36.9 s, turning a 30x gap into ~5%. Convergence at a fixed latency after the write fits equally well and would be near-definitional. The measurement stands without a mechanism; whether the repair clock is anchored to the write or the restart is the open question, and A's slow runs at ~56 s since-write rule out a single global constant. Condition A (6 s outage) is bimodal at ~0.02 s or ~52 s and is not explained by either manipulated variable; left open, no mechanism named. Also re-argued the "step, not a decay curve" conclusion from trajectories alone, since it had inherited from the withdrawn reading: it holds at 50–500 objects (transition in 8–167 ms) but not at 5,000 (5.8–6.3 s, ~5–6 points at 1 s cadence), so divergence size decides whether the dissociation experiment measures a step or a curve. See `research/weaviate_repair_window/SPEC.md` step 2c.

**Cancelled the sub-second sampling work that three merged PRs had named as the last prerequisite for the Weaviate experiment.**
PR #49 (`research/weaviate_repair_window/`, issue #48). #43 measured one async repair at ~0.3 s and #44/#45/#46 turned that single observation into a design constraint — "any Weaviate healing measurement must beat 0.3 s" — which made sub-second sampling the only thing standing between the instrument chain and the dissociation experiment. Characterizing the apparatus first, as `SPEC_TEMPLATE.md` now requires, showed the figure was one draw from a wide timing-determined distribution: the same 50-object divergence gives 44.7 s, 0.008 s, 0.010 s, and repair is independent of divergence size across 50→5,000. Sampling only ever had to beat the slow route, so **1–5 s cadence over a ≥60 s observation is sufficient and the sub-second work was never needed**. Logged here because it is a decision that stops a line of work, which this log requires be logged rather than left in a PR comment. The instrument-characterization requirement paid for itself on its first use: three prior sweeps (#11/#16, #20, #25) were voided by apparatus properties computable in advance, and this is the first time the check ran before the compute rather than after. A hard probe bound was also found by hitting it: `ids` is base64 in the URL, so a probe works to 15,000 ids and fails silently at 20,000. See `research/weaviate_repair_window/SPEC.md`.

**Made the Weaviate per-replica probe trustworthy before trusting it, and fixed a silent-topology hazard that would have invalidated every Weaviate run.**
PR #47 (`research/weaviate_probe_per_id/`, issue #46). #43's probe answered "how many bytes came back", which cannot support `completeness`, a metric defined over specific ids. Object ids turn out to be 16 bytes big-endian in the internal payload, so presence became a set rather than a count, validated against a *constructed* expectation — present, never-written, and peer-only ids in one request — because the failure mode that matters is a decoder that always succeeds. 8/8 checks, 0 false positives on never-written ids, and a diverged replica showing 0/10 peer-only ids while holding 20/20 others with peers up. Two things are recorded deliberately. **(1)** The first implementation asserted a superset rather than the pre-registered set equality; the review caught it, and the fix asserts equality at both moments where the expectation is actually defined (before and after peer-only convergence), since it is undefined across that boundary. **(2)** `create_class` tolerated HTTP 422 and silently ran against a stale auto-schema class that was **3 shards at replication factor 1** — sharded, not replicated — which would have produced per-replica numbers from a topology that had no replicas. `verify_class()` now checks the topology it got rather than the one it asked for. The image is pinned by digest for the same reason: the probe depends on an undocumented internal API of one specific build. See `research/weaviate_probe_per_id/SPEC.md`.

**Accepted an undocumented internal API as the Weaviate probe, making the Weaviate experiment asymmetric by design.**
PR #45 (`research/weaviate_nonperturbing_probe/`, issue #43). #42's isolation probe worked but left the probed node UNHEALTHY for ten minutes, so per-sample probing was impossible and the Weaviate leg was bounded to a snapshot. A cluster-internal API on `CLUSTER_DATA_BIND_PORT` (7947) returns one replica's own objects with peers up — 20/20 probes at 5 s cadence against the isolation probe's 1 — and it is provably local: it refuses when the node is down, and returned 0 bytes while peers returned 31,190 on the same request. `_search` on that port returns 415 across 12 content types, so **`index_recall` stays snapshot-only while `completeness` can be sampled continuously.** That asymmetry is accepted rather than engineered around, and the eventual Weaviate claim has to carry it: the data axis is a time series, the graph axis is two endpoints. The dependency is deliberate and stated — an undocumented API is not a contract, which is why #46 pins the image by digest. See `research/weaviate_nonperturbing_probe/SPEC.md`.

**Established that the per-replica protocol transfers to Weaviate at all, before designing anything that assumes it.**
PR #42 (`research/weaviate_probe/`, issue #41). Weaviate documents no node-targeting read, which put the whole cross-system generalization in doubt: the project's entire method is measuring one replica against its peers. Isolation probing — cutting a node off and reading it at `consistency_level=ONE` — works, and the isolated replica reported a divergence it had been shown (300 objects against peers' 400). Two negative findings are as load-bearing as the positive one. **(1)** `/v1/nodes` `objectCount` lags by minutes and is unusable as `completeness`, so the obvious cheap route is closed. **(2)** The probe left the node UNHEALTHY for ten minutes, which is what made #43 necessary. Recorded as feasibility-at-a-cost rather than feasibility, because the cost is what determined the next three issues. See `research/weaviate_probe/SPEC.md`.

**Fixed a harness defect that had already cost one pre-registered seed by producing a normal-looking run with no data in it.**
PR #40 (`research/qdrant_chaos_loop_timeout/`, issue #38). Seed 20261100's quiesce run in #35 fired zero kills across a 127 s chaos window and reported nothing wrong. Root cause: the chaos loops run in daemon threads, so the `TimeoutExpired` that `subprocess.run(timeout=30)` already raises killed the loop with no event, no log line, and a run that looked complete. A failed kill is now an event carrying `failed`/`error`/`timed_out`; `run_meta` carries `kill_count`/`kill_failures`/`chaos_no_kills`/`chaos_realized_s`; and `qdrant_sweep.py` refuses to accept a chaos run that fired no kills. 13 new checks, the 27 scheduler checks still passing, verified on a live run. Recorded because it is the third instance of the same class in this project — #26, #38, and the 20,000-id probe in #48 all failed *silently* — and silence is the failure mode this research is about, reproduced in its own instruments. See `research/qdrant_chaos_loop_timeout/SPEC.md`.

**Retired "Qdrant's healing is seed-inconsistent" as a horizon effect, and excluded one of five pre-registered seeds by a rule written when the case appeared.**
PR #37 (`research/qdrant_index_recall_healing/`, issue #35) re-ran PR #31's gated protocol with a 180s quiesce instead of 50s. Graph axis: the replica-level `index_recall` loss is a transient of the restart — 4 of 4 judged seeds inside their own baseline range over the last 60s, the largest post-kill dip 0.946 for a single 30s bin. Data axis: 100% of missing ids recovered in all four, where PR #6's 50s window had produced the 0-100% spread this log recorded as "healing is genuinely mixed." That spread was the window, not the system; `cross_system_replication/SPEC.md` carries the withdrawal beside its original text. Three things are recorded deliberately rather than smoothed. **(1) N.** Seed 20261100's quiesce run fired zero kills in a chaos window that ran 127s on a 50s setting; the analyzer reports it unmeasured and excludes it, by a rule added when the case appeared and before any number was used — 5 pre-registered, 4 judged. The harness defect behind it is #38: a blocked Docker call leaves no trace, because the loop records an event only after a kill completes. **(2) The closest seed is a tie.** 20261104 clears its baseline minimum by 0.0002 with later bins at 0.985-0.986 against 0.986 — "indistinguishable from baseline," not "recovered above it," on a metric with ~1% of headroom at k = 10 over 100k SIFT. **(3) The instrument has a duration limit.** At 240-250s and ~1.6k writes/s the un-indexed appendable tail keeps every replica near the 0.95 conditioning bar (baseline medians 0.936-0.954), so conditioning retained as little as 29% of one post-kill window; the unconditioned view agreed, which is why the result stands, but a longer gated run needs a threshold below 1,000 KB or a write pause. Also recorded: the review round on #37 caught the summary sentence calling a 0.003 dip (n = 3) "0.001 with n = 1-2" — the same-direction drift `claim_corrections/README.md` describes, in the same place it always is, caught the same way (reading the prose against the table). See `research/qdrant_index_recall_healing/SPEC.md`.

**Recorded the cross-system graph-quality result at the replica level, with the cluster-level null beside it, rather than as "Qdrant's graph diverges."**
PR #31 (`research/qdrant_gated_index_recall/`, issue #30) re-ran PR #6's Qdrant protocol with the corpus gated HNSW-indexed (PR #29's instrument, `indexing_threshold` 1,000 KB — the only setting at which a gate closes, since Qdrant's appendable segment otherwise leaves 85–93% of the corpus un-indexed) and five new seeds. The pre-registered metric — per-seed mean `index_recall` on the worst replica per round, conditioned on that replica ≥95% indexed — separates at the 5-vs-5 floor: 0.990 vs 0.978, p = 0.0079, every chaos seed below every baseline seed, the killed node the worst replica in 4 of 5 runs; every chaos-window round retained at least one replica at the bar, and 92% of rounds had every replica there. The unmodified `aggregate.py`, which averages over all six replicas, reports 0.992 vs 0.990, p = 0.31. Both are reported, and the README carries the unit in the claim, for the reason this log already gives for `loo_agreement`: aggregate cross-replica statistics collapse onto the healthy majority, and the operational question is which replica to distrust. Consequences: PR #6's null had two causes — the un-indexed corpus (2026-09-02 correction) and the cluster mean — and either alone would have hidden the effect; the top-level README's DO-NOT-CLAIM list now forbids the sentence without the unit, forbids a mechanism claim (what a kill does to a persisted HNSW is not observed), and keeps healing at HYPOTHESIS (2 of 5 seeds' worst-replica means below the baseline range within the 50s quiesce window after kills stopped, 4–5 samples each). Also recorded from the instrument work: run 0's contrast — `SearchParams.exact` scores 1.000 on every replica while the default path scores 0.996–0.999 on the same state — is how "indexed" was shown to mean "searched by the graph"; the first version of that check (score before vs after the gate) was uninformative because the gate closed in 2s, and is kept as such. Effect size is ~1.2 points on the seed mean inside the ~1% of headroom `index_recall` has at k = 10 over 100k SIFT: real, at the floor, and small — a harder query workload (open question #5) is the way to enlarge the scale, not more seeds. See `research/qdrant_gated_index_recall/SPEC.md`, `research/qdrant_index_gate/SPEC.md`, and the 2026-09-04 addendum in `research/cross_system_replication/SPEC.md`.

**Hardened the pipeline after an audit of every merged PR found the same defect class three times: `main` carrying something no review had looked at.**
The audit (2026-09-03, prompted by PR #27's review) found no PR that should be reverted, and three things the process had let through. (1) `research/cross_system_replication/results/{samples.csv,events.json,run_meta.json}` — the harness's scratch output — were tracked, unlike `replica_recall/results*/`; PR #18 committed them as a side-effect and PR #24 overwrote them with its last run, so `main` carried an unlabeled stale run for two merges. Neither PR body mentioned the files, and neither review asked. (2) PR #18 merged a head other than the reviewed one and lost two review fixes (#19 re-landed them and named the missing check; it stayed unimplemented). (3) PR #25's round-2 decision to stop the kill-spacing line existed only in a PR comment, and its index row still said "In progress." Changes, all to `AGENT_PIPELINE.md` unless noted: the Reviewer's first act is now a mechanical diff audit — file list, deletions, anything under `results*/` — compared against the PR body's own account, before reading prose; every MERGE decision names the head SHA it reviewed; a new query 0 verifies after each human merge that `main` contains that SHA and updates the index row, so "merged" is a checked state rather than an assumed one; decisions that stop a line of work must be logged here, not just commented; `SPEC_TEMPLATE.md` gains a required "Instrument characterization" section, because the three void or withdrawn sweeps (#11/#16, #20, #25) each failed on a property of the measurement apparatus that was computable from existing artifacts before the sweep ran; and the loop is on by default via a `SessionStart` hook, since PRs had been waiting on someone remembering to start it. The owner also decided that changes to the pipeline itself are committed straight to `main`, as editorial changes already are — research branches keep the full path.

**Merged the corrected kill-spacing branch (#24, PR #25) for its methodology, archived its result as VOID, and stopped the kill-spacing line of work.**
Recorded here on 2026-09-03 from PR #25's review, which had reached the decision the day before without a log entry. The pre-registered comparison failed its own Amendment 4: realized sampling interval 4.27s against a ≤4.0s precondition (10 of 15 runs over), and measured directly, 16 of 42 damage episodes reached three samples while 11 were single-sample. The primary metric showed no separation (p = 0.55 / 0.84 / 0.69); a secondary peak metric at p = 0.0317 was refused on three independent grounds, the strongest being that Amendment 3 had demoted it in advance for exactly this reason. Merged anyway, under the `graph_forensics.py` shape: `analyze_corrected.py`'s precondition path caught the void and its comparison path ran on real damage, and the branch produced the first characterization of the damage transient (lag from kill to visible damage median 14.1s, range 7.5–46.8s across 45 kills) and of the sampling floor (`ListLocalIds` shipping 81,775 ids × 6 replicas per round, in direct tension with the corpus size needed for writes to be in flight). Both survive the question they were built for. Stopped because two sweeps, ~2h15m of cluster time and zero findings were spent on a question the README's open-questions list does not rank, while ranked priority #1 (re-measure Qdrant `index_recall` with indexing front-loaded; a system with real anti-entropy) had named next steps untouched. If kill spacing is revived it re-enters through the researcher role competing on merit, not by inheriting priority from compute already spent. The cheaper completeness probe #25 proposed should still be built — for priority #1, which will hit the same floor.

**Archived rather than merged the controlled kill-spacing experiment (#9, PR #20): the pre-registered metric came back degenerate, and the analyzer had not earned the negative-result merge exception.**
The 15-run sweep completed with every validity precondition passing, but `damage` at chaos-stop was 0 in all 15 runs, so `recovered_frac` was 0/0 — undefined, not "healed perfectly." Cause, recorded in the spec that made the error: Amendment 3 held the chaos-window duration constant while varying spacing at fixed kill count, which cannot be done — the window is a function of the spacing. The short-gap condition therefore stopped killing at t~60s and idled 91s inside its own chaos window (against 21s for the others), and Qdrant repaired everything in that gap. Damage during chaos was real and large (peaks of 3,225–6,517 missing ids); it was simply gone before the measurement point. `GIT_WORKFLOW.md` lists "result uninterpretable" under when-not-to-merge, and its negative-results carve-out (the `graph_forensics.py` precedent) applies only when the implementation is validated infrastructure — which `analyze_kill_spacing.py` is not yet: its precondition path is proven, since it caught this degeneracy, but its comparison path has never produced a non-zero number and cannot be exercised on any dataset currently in the repo (pointed at `cross_system_replication/results_sweep`, the one dataset with real healing damage, it skips every run, since those are randomized and carry no condition field). `graph_forensics.py` merged because it worked and returned a negative answer; this tool has been shown only to correctly refuse. Branch `experiment/qdrant-kill-spacing` is preserved with its 15 runs and full writeup. Recorded exploratory and explicitly not established: peak and integrated damage-time both separate compressed kills from spaced and spread ones (p=0.0079–0.0159) while spaced and spread are indistinguishable from each other — pointing at "hit again before recovering" rather than "same node," which needs its own pre-registration rather than a citation from here.

**Withdrew the cross-system "Qdrant diverges in data completeness but not graph quality" finding in both directions, rather than reversing it or footnoting it.**
PR #6's 5-seed Qdrant sweep reported the `index_recall` null as an established cross-system difference from nano-db. PR #11 then instrumented Qdrant's own `/collections/{name}` telemetry at that sweep's scale and found the collection reported zero indexed vectors on every node for the whole baseline period and most (60% of samples, seed 20260920) or all (84%, seed 20260921) of the chaos window. Qdrant serves un-indexed segments by exact scan, so `index_recall` in that window was ~1.0 by construction, with no approximate graph in the loop to damage — an absent measurement, not a null result. The correction says the axis is **untested**, not that Qdrant's graph does diverge: PR #11's own runs never finished indexing either, so no positive claim is licensed in either direction, and overcorrecting to "Qdrant's graph does degrade" would repeat the original error with the sign flipped. The original text is struck through and annotated rather than deleted, per `GIT_WORKFLOW.md`'s rule against rewriting history to look linear — the transferable lesson is not the corrected sentence but the failure mode: a metric can quietly measure something other than what it is defined to measure when an un-instrumented system behavior (here, indexing lag at the write rate and scale used) changes what the query engine is actually doing. The related protocol fix — front-load indexing, or lower `indexing_threshold`, so measurement starts against a built graph — is a `method/*` change and deliberately not folded in. See `research/cross_system_replication/SPEC.md`'s correction addendum and `research/qdrant_optimizer_masking/SPEC.md`.

**Gave the pipeline's role selection a query for `stage:changes-requested` PRs, and capped the review/revision cycle at three rounds rather than letting it run unbounded.**
The original query list had no condition matching a PR the reviewer had sent back: query 1 looked for `stage:in-review` PRs and query 2 for unassigned `stage:proposed` issues, while a revised PR's linked issue is both assigned and out of `stage:proposed`. Requested revisions were therefore invisible to every tick, and the loop would have moved on to filing new research questions with work in flight unaddressed — the inverse of the downstream-first priority the section claims to implement. Found the first time a review actually returned REVISE (#12), not by reading the document. The round cap exists because queries 1 and 2 now hand one PR between two roles played by the same session, which nothing else bounds; it escalates to a human rather than auto-approving, because auto-approving at the cap would resolve a two-roles-converging failure in precisely the direction the merge gate exists to prevent. On #12 all three rounds found real defects, so the cap is not aimed at productive rounds — it is aimed at the case where they stop being productive. See `research/AGENT_PIPELINE.md`.

**Narrowed the top-level README's Layer 3 query-workload caveat to the axis that is still open, instead of leaving "does `loo_agreement` survive non-pinned traffic?" listed as unanswered after it had been answered.**
PR #7 tested it: three 5-seed conditions (pinned, non-pinned/100 queries per round, non-pinned/15) give mean hit rates of 0.87 / 0.86 / 0.81 against a 1/3 chance line, with per-seed values spanning 0.65–1.00 across the three and overlapping heavily, with all nine pairwise between-condition comparisons non-significant (p = 0.15–0.90). Open question #5 was rewritten to the part that remains untested — a query workload drawn from a genuinely *different distribution* than the corpus's own, or an adversarial one — rather than deleted, because finding no detectable pinning effect at two query counts is not the same as showing the detector is workload-shape-invariant, and a deleted item reads as the stronger claim. The result was recorded under HYPOTHESIS, not promoted to ESTABLISHED: at 5 seeds per condition the test is a weak instrument, so it weakens a confound on the Layer 3 claim rather than eliminating it, and does not confirm the claim, which still rests on one system and one implementation. A matching DO-NOT-CLAIM line was added for the same reason — with #5 gone from the open list, a reader could otherwise infer general workload robustness from its absence. Detection accuracy has no meaningful no-chaos baseline to be compared against (with nothing degraded, the detector's target is undefined — `aggregate.py` reports it as such), so "beats chance" here means the 1/n line, not a baseline-vs-chaos separation like the other metrics use. Three rounds of review were needed to get this entry's own wording as bounded as the claim it records — each round's overstatement ran in the direction of a tidier result, which `research/claim_corrections/layer3-query-pinning.md` records as the more durable finding than any of the corrections. See that file and `research/loo_agreement_nonpinned_queries/SPEC.md`.

**Retired the three-concurrent-session model in `research/AGENT_PIPELINE.md`; one session now rotates researcher/implementer/reviewer by reading GitHub state each tick.**
Three independent sessions gave the strongest possible role separation — a researcher session literally could not see the implementer's reasoning — but that separation was purchased with real operational cost: three things to schedule, three context windows, no single place to look at pipeline state. With the label taxonomy and PR/issue templates already durable and auditable in their own right, the separation can be enforced by rule (re-derive state from GitHub, never trust in-session memory of a prior role, never review your own PR) instead of by which account is logged in. Merge stays manual regardless — GitHub itself refuses self-approval on a PR opened by the same account, and that's the one step whose whole point is a check external to whoever produced the thing being checked. See `research/AGENT_PIPELINE.md`.

**Renamed the repository `Nano-DB-Replica-Recall` → `Replica-Recall-Divergence`, at the moment the `research/cross-system-replication` branch was opened.**
The `Nano-DB-Replica-Recall` name was deliberately chosen to be non-overclaiming — it could never be misread as a general claim, because the system was in the name. That reasoning stops being the right tradeoff the moment the project starts actively testing generalization to a second system: a Qdrant experiment living in a repo literally named after Nano-DB would undersell a positive cross-system result. Renamed to the phenomenon's actual name instead, ahead of any cross-system evidence existing — the non-overclaiming work now lives entirely in the README's ESTABLISHED/HYPOTHESIS/OPEN/DO NOT CLAIM framing rather than in the repo name, which is the more durable place for it to live regardless of which system(s) the project ends up covering.

**Adopted the research-branch workflow in `GIT_WORKFLOW.md`.**
Prior work (the original recall-bug investigation, the replica-recall Layer 1 measurement, the catastrophic-disconnection investigation) was all done directly on `main` or on branches that were merged and deleted without a formal spec-first process. That work is real and stays in the historical record as-is — this decision is about *future* branches, not a retroactive relabeling of what already happened. See `GIT_WORKFLOW.md`.

**Restructured the repository to be research-first.**
The repository read as a systems/portfolio project with a research finding mentioned three paragraphs into the README. Restructured so the README leads with the research question and an ESTABLISHED/HYPOTHESIS/OPEN/DO NOT CLAIM breakdown; Nano-DB-the-database moved to an appendix. No research question, methodology, or claim changed — see the commit `restructure: research-first repository layout` and its message for the full rationale. Repository later renamed `nano-db-replica-recall` → `Nano-DB-Replica-Recall` per an explicit user naming convention (title-case per hyphenated segment, acronyms stay capitalized).

**Ruled out pure concurrency and the duplicate-insert bug as the mechanism behind the 58.7%-loss anomalous replica, rather than accepting either on inference.**
Both were plausible from reading the code. Both were tested with deterministic, isolated reproductions (16 concurrent writers / zero chaos for the first; a scripted duplicate-insert-then-bulk-insert for the second) rather than accepted because they were consistent with the observation. Both came back clean. The duplicate-insert bug was real anyway (silently overwrote a vector's data on re-insert) and was fixed regardless of not explaining the anomaly — a bug doesn't need to explain your headline result to be worth fixing. Root cause of the anomaly is still open. See `postmortems/catastrophic-disconnection.md`.

**Fixed the duplicate-insert bug as "no-op if the incoming data is identical, reject if it differs" rather than always rejecting.**
`RemoveShard`'s rebalance path is documented as idempotent and can legitimately re-migrate a key that already landed on a shard — an unconditional reject would have broken that. See `cluster/shard_service_impl.hpp` and the fix commit.

**Changed `node_locks_` from a growing `std::vector` to a fixed 4,096-entry striped pool.**
The growing-vector design had the lock array's growth nested inside the storage-resize branch, so it silently stopped growing once storage pre-allocation covered enough nodes (the original recall-bug root cause). The fix wasn't "grow it correctly" — a fixed striped pool removes a second latent bug (reallocating a vector while other threads index into it is a race) at the same time. See `postmortems/recall-bugs.md`, Bug 2.

**Chose real SIFT1M over uniform-random synthetic vectors as the corpus for the replica-recall experiment.**
Uniform-random 128-d vectors suffer distance concentration — the true top-k becomes close to arbitrary, and recall falls with N for reasons unrelated to replication. Measured directly: `index_recall` showed no separation between baseline and chaos on uniform data (p = 0.31) but separated cleanly on SIFT1M (p = 0.0079). This was tested, not assumed — a benchmark built on the wrong corpus would have concluded there was nothing to find. See `research/replica_recall/README.md`, "Choosing a corpus."

**Chose the exact two-sided Mann-Whitney U test over a t-test for baseline-vs-chaos comparison.**
Five runs per condition is too few to lean on a normality assumption, and the metrics (recall, completeness) are bounded proportions, not naturally normal. Rank-based avoids that assumption. Tradeoff accepted and documented: at n=5 vs n=5 the smallest attainable p-value is 2/252 ≈ 0.0079, so a "significant" result at that floor indicates the groups separate completely, not that the effect is necessarily large — judge magnitude from the means, significance from the p. See `research/replica_recall/README.md`, "The seed sweep."

**Probes bypass the coordinator and hit each replica's gRPC port directly.**
Going through the coordinator would merge replica responses via scatter-gather, which is exactly the mechanism that would hide the divergence under study. This is a deliberate deviation from how a real client would query the cluster, made because the research question is about per-replica state, not client-observed behavior.

**Score `loo_agreement` per-replica (leave-one-out) rather than using aggregate shard-level agreement as the detection signal.**
Aggregate cross-replica agreement is close to tautological: when every replica misses the same hard queries, agreement collapses onto recall whether or not anything is broken, and it reads ~1.0 on a healthy cluster too — it doesn't discriminate. Per-replica peer agreement asks the sharper, operationally relevant question: which replica should I distrust. See `research/replica_recall/README.md`, "What is measured."

**Judge the healing/quiesce result on absolute missing-write count, not on the `completeness` ratio.**
`completeness` is a ratio, and writes keep flowing during the post-chaos observation window — a growing denominator drags the ratio toward 1.0 even when zero missed writes have actually returned (documented concretely as "the dilution trap": completeness climbed 0.9607 → 0.9694 while the absolute missing count stayed flat at 592 across three post-chaos windows). Using the ratio would have reported false recovery. See commit `research: judge healing on absolute missing writes, not the ratio` and `research/replica_recall/README.md`, "The dilution trap."

**Never read graph-forensics data off a live cluster; always tear it down first (`forensics_experiment.py`).**
`element_count` in the on-disk header lags the actual node writes by design (the node body is persisted before the counter is bumped), so a mid-write snapshot of a live cluster looks exactly like corruption. The first forensics attempt measured a running cluster and reported 168 dangling edges that turned out to be nothing. See `postmortems/catastrophic-disconnection.md`.

**Removed the ClickHouse #104674 citation from `RELATED_WORK.md` rather than softening it.**
It had been the lead production-evidence item for the silent-failure motivation. A verification pass reading the full issue thread found the reporter had retracted their own report — the ANN/brute-force disagreement was a bf16-quantization precision artifact reproducing on a clean server, not a durability bug, and there was no maintainer dismissal to cite. A citation that sounds too convenient was checked and found wrong; it was removed rather than reworded to sound less strong, because the underlying fact no longer supports any version of the claim. See `research/RELATED_WORK.md`, §6.

**Walked back the "ground-truth-free" description of Dimitropoulos et al.'s `1/Ratio@k` metric to "judge-free."**
An earlier draft of `RELATED_WORK.md` called it ground-truth-free; on rereading, the metric is defined over differences between retrieved and *true* nearest-neighbour distances, so it still requires exact ground truth to compute. "Judge-free" (no LLM/human judge in the loop) is a real but different property. Corrected rather than left, since the distinction matters for exactly the property this project's own detector needs. See `research/RELATED_WORK.md`, §5.

**Corrected a misattributed author on the STTT verification citation** (Zhang is a coauthor of Adhikari et al., not first author, in an earlier draft's citation of the quasi-linearizability verification work). See `research/RELATED_WORK.md`, §1.
