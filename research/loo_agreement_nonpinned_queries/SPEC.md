# Spec: experiment/loo-agreement-nonpinned-queries

**Branch:** `experiment/loo-agreement-nonpinned-queries`
**Date opened:** 2026-08-23
**Status:** COMPLETE — closed on a clean null across three 5-seed conditions; `--loo-query-mode` merged as infrastructure. *Header corrected 2026-09-06: this said DRAFT while carrying Results, Interpretation and a Decision.*

Issue: closes #5. Body copied verbatim below (per `research/AGENT_PIPELINE.md`'s implementer instructions — this is the issue text unmodified, not a paraphrase).

## Research question

Does `loo_agreement`'s ability to identify the degraded replica (measured in Layer 1/3 against a pinned, seeded query set) survive when the probe query workload is instead drawn from a non-pinned, realistic distribution (e.g. resampled per query round from the corpus's own query log / a held-out slice of SIFT1M's query set, or freshly sampled each round rather than fixed) — or does its above-chance detection accuracy collapse once queries stop being identical across replicas and across samples?

## Hypothesis

`loo_agreement`'s detection signal partially depends on the query set being pinned and identical across replicas: with a fixed query set, all healthy replicas are being asked to agree on the same hard/easy queries, which sharpens any real disagreement caused by one replica's data loss or graph degradation. Once queries vary per sample (still drawn from the same true distribution, but not identical draws), some of that agreement/disagreement will be swamped by ordinary per-query variance across differently-sampled query sets, so detection accuracy above chance should still hold but with reduced discriminative power (a smaller effect size / lower separation) versus the pinned-query result already established.

## Null / alternative hypothesis

Null: `loo_agreement` computed under a non-pinned query workload identifies the true lowest-`index_recall` replica at a rate statistically indistinguishable from chance (1/n replicas per shard, per the existing Q4 chance-baseline framework in `research/replica_recall/README.md`), i.e. the detector's above-chance performance was an artifact of query pinning rather than a property of the underlying agreement signal. Alternative (as hypothesized above): non-pinned `loo_agreement` still beats chance, but with a measurably smaller gap between detection accuracy (or the underlying agreement-score separation between the true degraded replica and its peers) than the pinned-query condition — a quantifiable robustness cost, not a binary pass/fail.

## Motivation

This is item #5 on the top-level README's "Open research questions / next experiments" list ("Detector robustness — does `loo_agreement` still work against non-pinned, realistic query traffic?"), and it targets a load-bearing methodological choice that is currently untested: `research/replica_recall/README.md` states plainly that "the query set is pinned and seeded. Identical at every sample and across runs, so recall differences are attributable to the cluster, not the queries" — this was a deliberate design decision to isolate the cluster's contribution to divergence, but it also means the current Layer 3 result ("can a ground-truth-free peer-agreement signal detect the degraded replica?") has only been shown to hold under a query workload no real production system would ever serve. If `loo_agreement` is going to be proposed as a production detector (the project's stated Layer 3 goal), it needs to survive the traffic shape it would actually see. This doesn't touch replication, the fault model, or the dataset — it isolates the query-distribution axis specifically, per `GIT_WORKFLOW.md`'s isolation rule.

## Experimental design

System: nano-db, same 2-shard × 3-replica topology as the existing Layer 1 experiment. Same node-kill chaos protocol (`chaos_harness.py`), same corpus (SIFT1M), same settling window and baseline-first protocol as `research/replica_recall/run_experiment.py`. The only change: replace the pinned, seeded query set with a non-pinned query workload for the `loo_agreement` probes specifically — concretely, hold out a slice of SIFT1M's own query set (distinct from any queries used to compute `index_recall`/`e2e_recall` ground truth) and draw a fresh random sub-sample of that held-out pool at each probe round, rather than reusing one fixed set. `index_recall`, `completeness`, and `e2e_recall` keep using their existing ground-truth-backed pinned protocol unchanged (they need a fixed query set to be comparable across samples) — only the queries fed into the `loo_agreement` computation change. Run the same 5-seed sweep (baseline + chaos) used for the existing headline result, so this experiment's numbers are directly comparable to the already-established pinned-query numbers.

## Metrics

Primary: `loo_agreement`'s detection accuracy — does the lowest-scoring replica by `loo_agreement` match the true lowest-`index_recall` replica, measured per sample, aggregated across the 5-seed sweep, against the existing 1/n chance baseline (same test used for the current Q4 result). Secondary: the magnitude of separation between the degraded replica's `loo_agreement` score and its healthy peers' scores (effect size, not just a win/loss count), to compare directly against the pinned-query condition's separation rather than only a pass/fail chance-beating claim.

## Baselines / controls

(1) The existing pinned-query `loo_agreement` result from the Layer 1/3 experiment, already established in this repo, as the direct comparison point — same topology, same chaos protocol, same corpus, only the query-selection protocol differs. (2) A no-chaos baseline run under the new non-pinned query protocol, to establish the noise floor for the new setup on its own terms (ordinary ANN nondeterminism plus query-sampling variance, absent any real degraded replica) before comparing it against the chaos condition. (3) The 1/n chance baseline for detection accuracy, as already used for Q4.

## Expected outcomes

(a) Non-pinned `loo_agreement` still beats chance with separation comparable to the pinned-query result — pinning wasn't load-bearing, and the detector is more production-ready than assumed. (b) Non-pinned `loo_agreement` beats chance but with measurably smaller separation than pinned — the hypothesized outcome — meaning the detector is usable but degraded by query variance, which would motivate a follow-up on how large a query sample per round is needed to recover the pinned-query separation. (c) Non-pinned `loo_agreement` performs at or near chance — the detector's above-chance result depended on query pinning as a confound, and the current Layer 3 "hypothesis under active investigation" claim in the top-level README would need to be walked back to "detection works only under a pinned, non-representative query workload," a materially different (weaker) claim than currently stated. (d) Results confounded by an insufficient held-out query pool size relative to SIFT1M's query set (too few distinct queries to sub-sample meaningfully across 5 seeds × multiple rounds), which would need to be ruled out before trusting (a)/(b)/(c).

## Interpretation plan

(a) would mean the current DO-NOT-CLAIM caveat about generalization to "production deployments" can be narrowed slightly — the query-pinning caveat specifically would no longer need to be carried, though everything else in DO NOT CLAIM (single system, single implementation) still stands. It would NOT mean the detector generalizes beyond nano-db or beyond SIFT1M-shaped query distributions. (b) would mean `loo_agreement` is a real but weaker production signal than the pinned-query numbers suggested, and would motivate a follow-up experiment on query-sample-size sensitivity (how many queries per round are needed to close the gap) rather than abandoning the detector. (c) would mean the Layer 3 hypothesis needs to be reframed in the README as "detection under a pinned query workload only" rather than left as an open general hypothesis, and would raise the priority of finding a workload-robust alternative before any production-detector claim is made. It would NOT mean peer-agreement detection is impossible in general — only that this specific pinned-query instantiation doesn't transfer. (d) would mean the experiment needs a larger or synthetically-extended held-out query pool before any of (a)/(b)/(c) can be trusted, and should be reported as inconclusive rather than folded into a directional claim.

## Confounds considered

Held-out query pool size: SIFT1M's own query set is finite (10,000 queries); sub-sampling from too small a held-out slice across many rounds/seeds could itself reintroduce near-pinning by exhausting the pool's diversity, which would bias toward outcome (a) for the wrong reason — needs to be checked explicitly (e.g. reporting how much overlap exists between rounds) rather than assumed away. Ground-truth leakage: the held-out query slice for `loo_agreement` must not overlap the queries used to compute `index_recall`/`e2e_recall` ground truth, or a change in one metric could spuriously correlate with the other through shared queries rather than through the phenomenon under test. Statistical floor: this inherits the existing n=5 seed-sweep floor (exact Mann-Whitney at 5v5 bottoms out at p=0.0079), so — per the existing project convention — effect sizes and per-run separation should be reported alongside, not instead of, any significance test. Distributional mismatch: if the held-out query slice happens to differ systematically in difficulty from the query set used for the existing pinned result (e.g. by chance drawing easier or harder queries), an observed change in separation could reflect query-set difficulty rather than the pinned-vs-non-pinned manipulation itself — mitigated by drawing the held-out pool from the same SIFT1M query distribution and, ideally, checking that per-query difficulty (e.g. mean k-NN distance) is comparable between the pinned set and the held-out pool.

---

## Results

**Scope of what actually ran, stated up front:** what follows immediately below is this branch's original single-seed pilot (kept as-is, historical record — its headline number does **not** hold up at the pre-registered N; see the dated addendum further down, which is what the Decision is actually based on). Implementation: `research/replica_recall/run_experiment.py` was extended (not forked/duplicated) with a `--loo-query-mode {pinned,nonpinned}` flag, `--loo-queries`, and `--loo-pool-size`. `pinned` is the default and is a no-op change — with the flag omitted, `sample_once()`/`sampler_loop()` take the exact code path they took before this branch, byte-for-byte (verified: a `--dist uniform` smoke test under the unmodified default path completed correctly, matching prior runs' shape). `nonpinned` adds a second, independent probe round trip per replica per sample against a query set redrawn every round from a held-out pool reserved (via `RetainingWriter.make_queries()`, disjoint by construction from the ground-truth query indices) at run start — implementing the ground-truth-leakage control from the Confounds section directly in code rather than only asserting it holds. `metrics.py`'s `pairwise_agreement`/`leave_one_out_agreement` are unmodified; only which observations get fed into them changed, per the isolation rule.

**Pilot run** (`results/samples_{pinned,nonpinned}_pilot_seed20260900.csv` + matching `run_meta*`/`events*.json`): real SIFT1M, 100,000 base vectors, seed 20260900, `--queries 100 --k 10`, 20s pre-chaos / 60s chaos / 70s quiesce, run twice back-to-back with identical arguments except `--loo-query-mode` (`nonpinned` additionally used `--loo-queries 100 --loo-pool-size 3000`). The two runs are **not** bit-identical beyond the query-mode flag — chaos event timing differs slightly (9 vs. 8 chaos events) because `chaos_harness.chaos_loop`'s random draws come from Python's global `random` module, which is also consumed by the writer threads' `random.choice(node_ids)` calls at real, non-deterministic thread-scheduling timing; `--seed` reseeds the same seed at the start of each run but does not make the two runs' chaos schedules identical. This is a pre-existing property of the harness, not introduced by this branch — recorded here because it matters for reading the comparison below as "run under matched *protocol and seed*," not "run under an identical chaos trajectory."

Detection stats via `analyze.py`'s existing `_detection_stats()` (reused unmodified — the CSV schema is unchanged, `loo_agreement` is still just a column), same tie-exclusion logic as the current Q4 result:

| | pinned | nonpinned |
|---|---|---|
| scored groups | 24 | 25 |
| hit rate (detector picks true-worst replica) | **1.00** | **0.48** |
| chance baseline (1/3 replicas) | 0.333 | 0.333 |
| lift over chance | 3.0x | 1.44x |
| rank correlation (loo_agreement vs. e2e_recall) | 0.979 | 0.740 |
| `loo_agreement` pool coverage | n/a (pinned) | 21 rounds, 1562/3000 (52%) of the held-out pool drawn — well short of exhaustion, so the "pool too small, re-pinning through the back door" confound did not materialize in this pilot |

Beyond the headline hit-rate table, I also computed the actual detector *signal strength* directly — for every scored group, `loo_agreement`(true-worst replica) minus the mean `loo_agreement` of its peers (negative = correctly scores the true-worst replica lower, magnitude = separation) — since the hit-rate table alone collapses a large, confident gap and a razor-thin one into the same "hit." Pinned: mean gap −0.0255, median −0.0295. Nonpinned: mean gap −0.0151, median −0.0050 (i.e. more than half the nonpinned "hits" were near-ties, correct only by a hair). A Mann-Whitney U test on these per-group gap values, pinned vs. nonpinned, gives U=146.5, **p=0.0022** — a real, not noise-level, difference in signal strength between the two conditions in this pilot.

**This Mann-Whitney test is explicitly not the pre-registered one.** The Metrics/Confounds sections above call for a 5-independent-seed comparison; groups *within* one run are samples over time along a single, shared chaos trajectory and are not independent draws the way five separate seeded runs would be — reporting it regardless because it is informative as a within-run exploratory statistic, not because it substitutes for the actual test.

## Interpretation (pilot only — superseded, see addendum)

This single-seed pilot lands closest to **Expected outcome (b)**: non-pinned `loo_agreement` still clears the chance baseline (0.48 vs. 0.333, a genuine detector, not noise) but with a substantially smaller margin than the pinned condition (1.44x lift vs. 3.0x), a lower rank correlation with the true recall ordering (0.740 vs. 0.979), and a signal-strength gap that a within-run Mann-Whitney test distinguishes from the pinned condition's at p=0.0022. Concretely: query pinning was doing real, measurable work in the original result — it was not merely a design convenience, and about half of the pinned condition's advantage over non-pinned in this pilot shows up as thinner (near-tie) margins rather than outright misses, meaning production deployment of this detector as currently specified should expect a meaningfully weaker signal than the pinned-query numbers advertise, not a comparable one.

**What this single-seed pilot does and does not establish.** It establishes that the effect is real and large enough to show up cleanly in one run — this is not exploring whether there's *any* signal, both conditions clear chance. It does **not** establish the magnitude as a stable, generalizable number: n=1 seed means the 3.0x-vs-1.44x lift comparison could itself be seed-dependent, there is no cross-seed variance estimate, and the within-run Mann-Whitney's p-value should not be read as answering the pre-registered between-seed question the spec commits to. It also does not distinguish *why* the signal weakens — the Confounds section's "distributional mismatch" possibility (the held-out pool differing in per-query difficulty from the pinned set by chance) has not been checked directly (e.g. comparing mean k-NN distance between the two query pools), so query-count variance and query-difficulty variance are both still live explanations, not disentangled from each other.

**This section's read did not survive the 5-seed sweep below — kept verbatim rather than rewritten, because a wrong-but-honest pilot interpretation is part of the record, not something to quietly correct away.**

## Addendum: 2026-08-23 (later) — the actual 5-seed sweep, and a reversal

Per the reviewer's `stage:changes-requested` comment, this addendum records the pre-registered protocol: 5 seeds (`20260900`-`20260904`), pinned and nonpinned conditions, matched scale/duration (`--dist sift --sift-vectors 100000 --queries 100 --k 10 --duration 90`, both conditions run baseline+chaos per seed). Orchestrated with the existing `../replica_recall/sweep.py` unmodified — it forwards unrecognized flags straight through to `run_experiment.py`, so `--loo-query-mode nonpinned --loo-queries 100 --loo-pool-size 3000` needed no changes to the sweep tool itself, only to what gets passed after `--`. Analyzed with `../replica_recall/aggregate.py` (also unmodified) for the standard baseline-vs-chaos table per condition, plus a new `compare_conditions.py` (this branch) for the actual pinned-vs-nonpinned comparison across the 5 seed-level summaries, which is neither aggregate.py's per-condition table nor the pilot's within-run statistic. Raw output for all 20 runs (`results_sweep_loo_{pinned,nonpinned}/`) and both aggregate reports (`results/aggregate_{pinned,nonpinned}.txt`) are committed.

One infrastructure failure during this sweep, recorded rather than quietly retried away: an over-eager `set -e` in the first attempt's orchestration wrapper, combined with an ephemeral (`--rm`) container and a single transient `[rr] FATAL: no raft leader elected` on one run (seed 20260904 baseline, nonpinned condition — plausible under host resource contention from running this sweep concurrently with the sibling cross-system-replication branch's Qdrant sweep), discarded that attempt's completed runs before they were copied out of the container. No experimental data was corrupted or misreported by this — nothing had been analyzed yet when it happened — but it cost a full rerun. The rerun script copies results out of the container incrementally after each condition's sweep rather than only at the very end, so a repeat of this specific failure mode can't recur.

**The between-condition comparison** (`compare_conditions.py`; exact 5v5 Mann-Whitney, floor p=0.0079), extracting each condition's 5 chaos-run seed-level `_detection_stats()` summaries via `aggregate.py`'s own `discover()`:

| metric | pinned (mean ± sd) | nonpinned (mean ± sd) | p |
|---|---|---|---|
| detection hit rate | 0.8703 ± 0.1426 | 0.8602 ± 0.1434 | 0.9048 |
| rank correlation | 0.9181 ± 0.0865 | 0.8258 ± 0.1201 | 0.2063 |
| true-recall margin | 0.0706 ± 0.0197 | 0.0566 ± 0.0194 | 0.3095 |

Per-seed hit rates, since the means alone hide how much they overlap: pinned = [0.75, 1.00, 1.00, 0.909, 0.692], nonpinned = [0.867, 0.684, 0.75, 1.00, 1.00]. The two distributions interleave almost completely — the pilot's own seed, 20260900, actually scored *higher* under nonpinned (0.867) than several other pinned seeds scored under pinned (0.75, 0.692).

Each condition's own baseline-vs-chaos separation, from `aggregate.py`, for completeness: pinned — `completeness`/`e2e_recall` separate at p=0.0079, `index_recall` does not (p=0.0952); nonpinned — all three separate at p=0.0079. (This index_recall difference between conditions is itself interesting but is not this issue's question and is not investigated further here.)

## Interpretation (current — supersedes the pilot section above)

**None of the three between-condition comparisons reach significance, and the pilot's own dramatic hit-rate gap (1.00 vs. 0.48, p=0.0022 within-run) does not reproduce at the pre-registered N.** At 5 seeds, pinned and nonpinned hit rates are statistically indistinguishable (p=0.90) and the per-seed distributions overlap almost completely. This is closest to **Expected outcome (a)**, not (b) as the pilot suggested: pinning does not appear to be load-bearing for `loo_agreement`'s above-chance detection — both conditions clear chance (0.333) comfortably and by a similar margin, seed to seed.

**Why the pilot pointed the wrong way, as best this data can say:** per-seed hit-rate variance is large under *both* conditions (pinned ranges 0.692-1.000, nonpinned ranges 0.684-1.000) — large enough that a single seed drawn from either condition can land anywhere in that range. The pilot happened to draw a low-end nonpinned seed (0.48, actually lower than any seed in this 5-seed nonpinned sweep, so not even fully consistent with this sweep's own nonpinned distribution — worth flagging as a further reason not to over-trust it) paired with a high-end pinned seed (1.00). That combination, from one seed each, looked like a real, systematic gap; five seeds each show it wasn't. This is precisely the failure mode `GIT_WORKFLOW.md`'s spec-first discipline exists to catch — the pilot's hypothesis (b) was pre-registered as *plausible*, but the single-seed result that seemed to confirm it was, in retrospect, a seed-selection artifact, not the underlying effect.

**What this establishes and does not.** Establishes, at the pre-registered 5-seed floor: no detectable cost to `loo_agreement` detection accuracy from removing query pinning, under this fault model/scale/duration — outcome (a), not (b). Does **not** establish that pinning is *irrelevant* in general: this sweep used `--loo-queries 100`, matching the pinned set's size; a much smaller per-round query sample (a more aggressive "realistic" workload, e.g. 10-20 queries/round) is a different, untested condition and could behave differently — the Motivation section's underlying question (does the detector survive realistic traffic) is about workload *shape* broadly, and this sweep tested one specific shape. Also does not establish *why* index_recall separates under pinned but not nonpinned (noted above) — plausibly just seed-to-seed noise at n=5 given neither condition's index_recall p-value is far from 0.05, but not checked further.

## Decision

**REVISE**, but for different reasons than the pilot's REVISE. The implementation remains correct (unchanged from before — default path behaviorally identical, ground-truth-leakage control enforced in code, `metrics.py` unmodified). The evidence and experimental-validity criteria are now satisfied for the actual pre-registered comparison — this is the 5-seed sweep with a real between-condition test, not an anecdote — and the answer is a clean, if anticlimactic, **null result on the primary question**: outcome (a), pinning doesn't appear load-bearing at this scale. Per `GIT_WORKFLOW.md`'s "negative results" section, a clean null is not by itself a merge blocker, but two things still need attention before an ARCHIVE-with-negative-result or MERGE-as-infrastructure call:

1. ~~Run the actual 5-seed sweep~~ — **done**, this addendum. Result: outcome (a), not the pilot's (b).
2. The top-level README currently makes no claim yet needing correction from this branch (the pilot's finding was never merged into the README), so there is nothing to walk back — but if this branch's *nonpinned* mode is ever adopted as the default going forward (i.e. `run_experiment.py`'s headline runs switch off query pinning), that decision should cite this addendum's null result, not the pilot's now-superseded one.
3. Test at least one smaller `--loo-queries` value (the Interpretation section's open point) before concluding pinning is irrelevant *in general* — this sweep only tested "same query count, pinned vs. fresh," not "fewer queries per round."
4. The query-pool-difficulty confound (mean k-NN distance, pinned set vs. held-out pool) remains unchecked and is lower priority now that the headline effect it was meant to help explain didn't replicate.

Re-claim this branch or open a follow-on `experiment/*` branch for item 3 specifically, since it is the one open thread that could still recover something like the pilot's original (b) finding under a more aggressive (lower-query-count) nonpinned condition.

## Addendum: 2026-08-23 (later still) — a smaller `--loo-queries` doesn't change the answer either

Per Decision item 3, ran a third 5-seed sweep (same seeds, same scale/duration, same everything else) with `--loo-query-mode nonpinned --loo-queries 15` — an 85% reduction from the original nonpinned sweep's 100, meant to probe a more aggressive "realistic" workload (a client sending a handful of queries per round, not a hundred). `compare_conditions.py` was extended (this addendum) to take an arbitrary named set of conditions rather than a fixed pinned/nonpinned pair, so the same tool now covers all pairwise comparisons.

| metric | pinned | nonpinned (100 q) | nonpinned_small (15 q) |
|---|---|---|---|
| detection hit rate | 0.870 ± 0.143 | 0.860 ± 0.143 | 0.811 ± 0.096 |
| rank correlation | 0.918 ± 0.087 | 0.826 ± 0.120 | 0.827 ± 0.127 |
| true-recall margin | 0.071 ± 0.020 | 0.057 ± 0.019 | 0.088 ± 0.038 |

All three pairwise comparisons, all three metrics — 9 tests, none significant (p ranges 0.15-0.90). `nonpinned_small`'s hit rate (0.811) sits between the other two, not below both, and its margin is actually the *highest* of the three (though not significantly so). There is no sign of a "more aggressive nonpinned condition finally reveals the pinning-dependence effect" story at 15 queries/round any more than at 100.

## Interpretation (updated)

The one open thread from the previous addendum — whether a smaller per-round query count would recover something like the pilot's original signal — is now closed, negatively. Detection accuracy is stable across pinned, nonpinned-100, and nonpinned-15 at this scale and fault model. Combined with the previous addendum's finding, this is now three independent 5-seed conditions (15 total independent runs under chaos) all landing in the same 0.81-0.87 hit-rate band, none distinguishable from the others. This is a materially stronger basis for outcome (a) than a single pinned-vs-nonpinned comparison alone would be — the result isn't fragile to the specific nonpinned parameterization tested.

This still does not establish the detector is workload-shape-invariant in general (the Motivation section's broader question): all three conditions used SIFT1M's own query distribution for both the pinned set and the held-out pool, so a query workload that differs in *distribution*, not just *count*, from the corpus's own queries (e.g., a genuinely different embedding distribution, or adversarially-hard queries) remains untested and is a different, larger question than issue #5 scoped.

## Decision (updated)

**REVISE**, now with a considerably more complete negative result. Items 1 and 3 from the previous addendum's Decision are both done; only item 4 (query-pool-difficulty confound) remains, and it is now lower priority still — with three independent conditions agreeing, a distributional-mismatch explanation for *any single* condition's number looking different is less load-bearing than it was after only one comparison. Remaining:

1. ~~Run the 5-seed sweep~~ / ~~test a smaller `--loo-queries` value~~ — **both done**, this and the previous addendum. Consistent null across three conditions.
2. The query-pool-difficulty confound (item 4, previous addendum) — still technically open, now clearly lowest priority.
3. **New:** given how consistent this result is, the top-level README's Layer 3 language (if it currently implies detection depends on a pinned, non-representative workload) is worth a reviewer's read to check whether it should be updated to cite this branch's null result — not done here, since that's a README-thesis change and belongs in its own commit/discussion, not silently folded into this addendum.

Given three consistent negative results at n=5 each, this branch is close to ARCHIVE-as-negative-result territory per `GIT_WORKFLOW.md`'s negative-results section (the *implementation* -- the `--loo-query-mode` flag itself -- is the kind of thing that could still merge as validated infrastructure even though the headline hypothesis it was built to test came back null, per that section's `graph_forensics.py` precedent) — left as REVISE rather than self-assessing ARCHIVE/MERGE outright, since that call should go through the same review process as everything else on this branch, not be decided unilaterally in an addendum.
